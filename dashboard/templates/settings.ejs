<%- include("partials/header", { bot, user, path, title: `Settings | ${guild.name}` }) %>

<div class="container-sm center"  style="margin-top: 30px;">
  <div class="d-grid gap-2 col-4 mx-auto">
    <% if (alert) { %>
      <div class="alert alert-success" role="alert">
        <%= alert %>
      </div>
    <% } %>
    <center>
      <h1><%= guild.name %></h1><br />
      <%- guild.icon ? `<img src="https://cdn.discordapp.com/icons/${guild.id}/${guild.icon.startsWith('a_') ? guild.icon + '.gif' : guild.icon}" class="card-img-top" style="max-width: 18rem; border-radius: 25px">` : "" %>
    </center>
    <hr>
    <form method="POST">
      <center>
      <p class="h5" style="margin-bottom: 0.5rem; margin-top: 10px; border-radius: 0px 50px 50px 0px;">🛠️ Strona w przebudowie</p>
      <p class="h6" style="margin-bottom: 0.5rem; margin-top: 10px;"><i>Po wprowadzeniu komend z ukosnikiem ustawienia na ten moment nie mają sensu xD</i></p>
      </center>
    </form>
  </div>
</div>

<%- include("partials/footer") %>